# Core runtime dependencies. Source of truth is the [project.dependencies]
# block in pyproject.toml — keep this file in sync with it.
numpy>=1.26,<3
scipy>=1.11,<2
opencv-python>=4.9,<5
pydantic>=2.5,<3
PyYAML>=6.0,<7
click>=8.1,<9
