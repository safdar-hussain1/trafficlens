"""Analytics layer: derived quantities computed on top of tracking output.

``trafficlens.analytics.speed`` (plane-space speed estimation) imports only
the standard library and ``trafficlens.core`` so it can be mechanically
mirrored to TypeScript for the browser engine.
"""
