{
  "name": "trafficlens-web",
  "version": "1.0.0",
  "private": true,
  "description": "Browser engine for TrafficLens: the detector runs on the visitor's own GPU",
  "license": "MIT",
  "author": "Safdar Hussain <safdar.eryx@gmail.com>",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "onnxruntime-web": ">=1.27.0 <1.28.0"
  },
  "devDependencies": {
    "typescript": ">=7.0.0 <8.0.0",
    "vite": ">=8.2.0 <9.0.0",
    "vitest": ">=3.2.0 <4.0.0"
  }
}
